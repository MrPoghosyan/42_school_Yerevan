#include <stdio.h> // For printf()
#include "header.h"

/*
static void	push_swap(t_stack arr)
{
	///
}
*/
int	main(int argc, char **argv)
{
	if (argc > 1)
	{
		while (*argv)
		if (is_valid_integer())
	}
	else
		write(1, "\n", 1);
	return (0);
}
